# Baseline 标准与开源仓库参考

## 📌 现有开源 Baseline 仓库

### 1. OSSF Security Baseline

**定位**: 开源项目安全成熟度要求

**特点**:
- 控制项以 YAML 形式定义
- 可转换为 OSCAL/Markdown/checklist
- 机器可读结构

**GitHub**: https://github.com/ossf/security-baseline

**价值**:
- Baseline 标准化示例（用 YAML 表示静态控制项）
- 可参考字段结构与版本方案

### 2. ComplianceAsCode / CIS 基线

**定位**: 安全与系统配置规则库

**特点**:
- 大量 YAML 控制与规则定义
- 可转换成多种输出（脚本、SCAP、HTML）
- 规则丰富但偏系统/安全

**价值**:
- 可借鉴规则设计
- YAML 控制定义库参考

### 3. Kubernetes PodSecurity Admission

**定位**: Kubernetes Pod 安全基线策略

**特点**:
- 以 YAML 定义规范/规则
- 定义 Pod 的"最低安全配置要求"
- 成功实践案例

**示例**:
```yaml
metadata:
  annotations:
    pod-security.kubernetes.io/audit: baseline
```

### 4. Cloud Governance Baseline

**定位**: 云厂商策略资源

**特点**:
- 结构化规则以 JSON/YAML 表现
- 可版本化、自动化执行
- 多为 JSON 规则

## ❗ 当前开源仓库的定位与局限

| 类型 | 例子 | 说明 |
|------|------|------|
| 安全项目 Baseline | OSSF Security Baseline | 适合测项目安全成熟度 |
| CIS/Linux/Windows 安全基线 | ComplianceAsCode | 规则丰富但偏系统/安全 |
| Kubernetes 安全基线 | PodSecurity Admission | YAML策略定义 |
| Cloud Governance Baseline | 云厂商策略资源 | 多为 JSON 规则 |

## 🔍 本仓库的定位

本仓库（PRD Baseline）专注于：

✅ **业务领域 Baseline**（如日志容量、表清理、磁盘保护）
✅ **可用于 AI/OPA 审计的 domain-specific Baseline**
✅ **需求 → 工程 → 运维 → AI 的统一语言**

**现状**: 目前**没有统一、标准化、广泛接受的开源仓库**完全覆盖这些领域。

> 这也是为什么需要自建 Baseline 仓库。

## 🧠 借鉴现有开源 Baseline 结构

### 通用 Baseline 规则库结构（参考 OSSF & ComplianceAsCode）

```
baseline/
├── controls/                # 原子控制项（YAML）
│   ├── logging-capacity.yaml
│   ├── quota-limits.yaml
│   ├── disk-watermark.yaml
│   └── retention-policy.yaml
├── profiles/                # 根据使用场景组合规则
│   ├── enterprise.yaml
│   ├── private-cloud.yaml
│   └── oss.yaml
├── mappings/                # 关联外部标准
│   ├── owasp-asvs.yaml
│   ├── nist-800-63.yaml
│   └── cis-linux.yaml
├── tests/                   # 自动化规则测试集合
│   ├── logging-capacity.test.yaml
│   └── disk-watermark.test.yaml
└── metadata.yaml            # Baseline 仓库版本/元数据
```

## 📊 对比分析

| 方案类型 | 是否有开源仓库 | 是否可直接用于业务 Baseline | 本仓库的借鉴点 |
|---------|--------------|---------------------------|--------------|
| 安全基线 YAML（OSSF） | ✅ | ⭐ 可参考结构但非业务 | YAML 格式、版本管理、控制项结构 |
| CIS/配置基线（ComplianceAsCode） | ✅ | ⭐ 可借鉴规则设计 | 规则定义方式、测试框架 |
| Kubernetes PodSecurity Baseline | ✅ | ⭐ 规则格式示例 | YAML 策略定义模式 |
| 业务容量 Baseline（本仓库） | ❌ | 🔥 需要自建 | **本仓库的独特价值** |

## 🛠 本仓库的改进方向

基于开源标准的借鉴，本仓库可以：

1. ✅ **已实现**: YAML Schema 统一校验（`schema/baseline.schema.yaml`）
2. ✅ **已实现**: 外部标准映射（`mappings/`）
3. 🔄 **可增强**: Profiles 组合规则（按场景组合 Baseline）
4. 🔄 **可增强**: 自动化测试框架（`tests/`）
5. 🔄 **可增强**: 元数据管理（`metadata.yaml`）

## 📚 参考资源链接

- **OSSF Security Baseline**: https://github.com/ossf/security-baseline
- **ComplianceAsCode**: https://github.com/ComplianceAsCode/content
- **Kubernetes PodSecurity**: https://kubernetes.io/docs/concepts/security/pod-security-standards/

## 🎯 本仓库的独特价值

虽然现有开源仓库主要关注**安全/系统配置**，但本仓库填补了空白：

1. **业务领域 Baseline** - 日志容量、数据库容量、内存管理等
2. **需求层 Baseline** - 从 PRD 阶段就开始的 Baseline
3. **AI 友好** - 结构化 YAML，可直接被 AI 消费
4. **实现层映射** - 从抽象需求到具体框架实现（Logback、Log4j2、Zap 等）
5. **本地化/私有化部署优化** - 针对离线部署场景的特殊考虑

## 💡 总结

- ✅ 有通用安全 & 项目治理 Baselines（可作为模板或规则设计参考）
- ❌ 但没有针对日志容量 / 业务表容量 / 数据治理这种需求领域的通用 Baseline 仓库
- 🔥 **本仓库正在填补这个空白**
